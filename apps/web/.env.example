# Red de Stellar para la wallet. TESTNET por default; PUBLIC para mainnet.
NEXT_PUBLIC_STELLAR_NETWORK=TESTNET

# Lighthouse (IPFS) — server-side only, NUNCA exponer al cliente.
# Conseguí una en https://files.lighthouse.storage/ (Dashboard -> API Keys).
LIGHTHOUSE_API_KEY=
