{
  "name": "@molotov/indexer-db-tests",
  "private": true,
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest",
    "cloud-security": "tsx cloud-security.ts"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.0.0",
    "tsx": "^4.19.0",
    "vitest": "^3.0.0"
  }
}
